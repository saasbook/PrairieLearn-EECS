<html>
  <body>
    <div>
      <p> Choose a repository to synchronize: </p>
      <%= select_tag 'repo_id', options_for_select(@repos.map {|repo|
        [repo.full_name, repo.full_name]}), class: 'form-control' %>
      <br>
      <button class="btn btn-primary" type="button" id="sync">Synchronize</button>
    </div>
  </body>
</html>
